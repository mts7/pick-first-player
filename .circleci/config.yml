version: '2.1'

executors:
  java17:
    docker:
      - image: 'cimg/openjdk:17.0'

orbs:
  android: circleci/android@2.3

jobs:
  test:
    executor: java17
    steps:
      - checkout
      - android/run-tests
      - android/save-gradle-cache:
          cache-prefix: v1a
workflows:
  test:
    jobs:
      - test
